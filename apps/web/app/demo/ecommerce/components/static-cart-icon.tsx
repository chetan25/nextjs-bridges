export function StaticCartIcon() {
  return (
    <button
      disabled
      style={{
        display: 'flex',
        alignItems: 'center',
        gap: '0.4rem',
        padding: '0.5rem 0.75rem',
        background: '#fff',
        border: '1px solid #cbd5e1',
        borderRadius: 8,
      }}
    >
      🛒
    </button>
  );
}
